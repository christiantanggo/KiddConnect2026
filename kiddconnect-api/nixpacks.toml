# Node 20 + ffmpeg. One install path only: duplicate npm i + npm ci breaks on
# BuildKit cache at node_modules/.cache (EBUSY rmdir).
[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = []

[start]
cmd = "npm start"
