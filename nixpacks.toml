# Use Node 20 via .nvmrc + package.json "engines" (do not pin nodejs-18 here).
# ffmpeg needed for Orbix video.
[phases.setup]
nixPkgs = ["ffmpeg"]

[phases.build]
cmds = ["npm ci"]

[start]
cmd = "npm start"

