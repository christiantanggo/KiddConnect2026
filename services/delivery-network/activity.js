/**
 * Delivery request activity log (optional for MVP).
 * Stub: no-op; can later write to delivery_activity table.
 */
export async function logRequestActivity(deliveryRequestId, eventType, data = {}) {
  // Optional: insert into delivery_activity
  return;
}
